# 🔧 YOLO + C++ GPU 环境安装存档（傻瓜版复现指南）

> 本文档记录**所有成功配置**的操作步骤，照着一步步做即可完整复现这套环境。
> 不含任何失败尝试，只留有效信息。
> 适用硬件：Windows 11 + NVIDIA RTX 5060 Laptop (Blackwell, sm_120) + 8GB 显存

---

## 📋 最终环境全景

| 组件 | 版本 | 用途 |
|------|------|------|
| Python | 3.14.2（venv 虚拟环境） | Python 检测环境 |
| PyTorch | 2.11.0+cu128 | Python GPU 推理（10.85ms） |
| OpenCV | 5.0.0 (Python) / 5.0.0 (C++ 源码编译) | 图像处理 |
| Ultralytics | 8.4.114 | 官方 YOLO 库（YOLO26） |
| onnxruntime | 1.27.0 (C++/CUDA13) | C++ CPU 推理 |
| CUDA Toolkit | 13.3 | GPU 计算平台 |
| TensorRT | 11.2.1 | C++ GPU 推理（7.63ms） |
| MinGW g++ | 16.1 | C++ CPU 编译 |
| Visual Studio MSVC | 19.51 | C++ GPU 编译 |

**性能对比**：C++ TensorRT GPU 7.63ms < Python TensorRT 10ms < PyTorch GPU 10.85ms < C++ CPU 18ms

---

# 第一部分：Python YOLO 环境

## 1. 创建虚拟环境

打开终端（VS Code 终端），在项目目录执行：

```bash
cd "C:\Users\21404\Desktop\打工必备\各种作业跟项目\yololearn"
C:\Users\21404\AppData\Local\Programs\Python\Python314\python.exe -m venv venv
```

> 用系统稳定版 Python 3.14 创建（不要用 3.15 alpha，torch 不支持）

## 2. 升级 pip

```bash
venv\Scripts\python -m pip install --upgrade pip
```

## 3. 安装 GPU 版 PyTorch（关键：CUDA 12.8 版）

```bash
venv\Scripts\pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
```

> - `cu128` = CUDA 12.8 GPU 版（**必须选 GPU 版**，CPU 版是 `+cpu`）
> - RTX 5060 需要 CUDA 12.8+，cu128 正好满足
> - 约 2.5GB，下载需几分钟

**验证**：
```bash
venv\Scripts\python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
```
应输出 `2.11.0+cu128 True`（True = GPU 可用）

## 4. 安装 OpenCV + Ultralytics

```bash
venv\Scripts\pip install opencv-python ultralytics
```

> - opencv 5.0.0（图像处理）
> - ultralytics 8.4.114（官方 YOLO 库，含 YOLO26）

## 5. 下载 YOLO26 模型（自动）

运行任意检测命令即可自动下载模型到当前目录：
```bash
venv\Scripts\yolo predict model=yolo26n.pt source=bus.jpg
```

> - `yolo26n.pt`(5.3MB) / `yolo26s.pt`(20MB) / `yolo26m.pt`(43MB) / `yolo26l.pt`(51MB)
> - 模型自动从 GitHub 下载，下载一次永久使用

---

# 第二部分：C++ CPU 环境（onnxruntime）

## 6. 下载 onnxruntime 1.27（CUDA13 版）

从 GitHub 下载（需代理）：
```
https://github.com/microsoft/onnxruntime/releases/download/v1.27.0/onnxruntime-win-x64-gpu_cuda13-1.27.0.zip
```
解压到 `C:\cpp_msvc_build\ort127\`

## 7. 编译 OpenCV 5.0（C++ 源码版）

### 7.1 下载源码（放纯英文路径，中文路径会编译失败）

```bash
cd /c/opencv_build
git clone --depth 1 --branch 5.0.0 https://github.com/opencv/opencv.git src
```

> ⚠️ **必须放纯英文路径**（如 `C:\opencv_build\src`），中文路径会导致 NASM 汇编编译失败

### 7.2 配置构建（只编核心模块）

```bash
cd /c/opencv_build/src
C:\mingw64\bin\cmake.exe -G Ninja -S . -B build \
  -DCMAKE_CXX_COMPILER=C:/mingw64/bin/g++.exe \
  -DCMAKE_C_COMPILER=C:/mingw64/bin/gcc.exe \
  -DCMAKE_MAKE_PROGRAM=C:/mingw64/bin/ninja.exe \
  -DCMAKE_BUILD_TYPE=Release \
  -DBUILD_SHARED_LIBS=OFF \
  -DBUILD_LIST=core,imgproc,imgcodecs,videoio,calib3d,highgui \
  -DWITH_FFMPEG=OFF -DWITH_V4L=OFF -DWITH_GSTREAMER=OFF -DWITH_GTK=OFF \
  -DWITH_OPENCL=OFF -DWITH_CUDA=OFF -DWITH_OPENEXR=OFF \
  -DBUILD_opencv_apps=OFF -DBUILD_TESTS=OFF -DBUILD_PERF_TESTS=OFF -DBUILD_EXAMPLES=OFF \
  -DBUILD_opencv_python3=OFF -DBUILD_opencv_java=OFF \
  -DCMAKE_INSTALL_PREFIX=C:/opencv_build/install
```

### 7.3 编译并安装

```bash
cd /c/opencv_build/src
C:\mingw64\bin\ninja.exe -C build
C:\mingw64\bin\ninja.exe -C build install
```

> - 约 700 个编译目标，3-4 分钟
> - 安装到 `C:\opencv_build\install\`（含 include + 静态库 .a）
> - MinGW 版用 **MinGW 编译**；MSVC 版需单独用 MSVC 重编（见 8.3）

---

# 第三部分：C++ GPU 环境（TensorRT + MSVC）

## 8. 安装 CUDA Toolkit 13.3

### 8.1 下载

官网（需 NVIDIA 账号）：https://developer.nvidia.com/cuda-downloads
选择：Windows → x86_64 → **exe (local)** → 下载 `cuda_13.3.1_windows.exe`（2.4GB）

### 8.2 安装

1. 双击 exe → 安装选项选 **自定义（Custom）**
2. 勾选 **CUDA** 组件
3. **取消勾选 Driver**（你的驱动 610.88 已支持 CUDA 13，避免覆盖）
4. 安装到默认路径 `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.3`

> 装完后 `CUDA_PATH` 环境变量自动设置

### 8.3 编译 MSVC 版 OpenCV（C++ GPU 用）

C++ GPU 用 MSVC 编译，所以 OpenCV 也要 **MSVC 版**（不能用 MinGW 版）：

```bash
# 用 MSVC 环境（vcvars64）重新配置编译
# 在 PowerShell 里：先运行 vcvars64.bat，再用 VS 自带 cmake/ninja
& 'C:\Program Files\Microsoft Visual Studio\18\Insiders\VC\Auxiliary\Build\vcvars64.bat'
& 'C:\Program Files\Microsoft Visual Studio\18\Insiders\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe' -G Ninja -S C:\opencv_build\src -B C:\opencv_build\src\build_msvc -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=OFF -DBUILD_LIST=core,imgproc,imgcodecs,videoio,calib3d,highgui -DCMAKE_INSTALL_PREFIX=C:\opencv_build\install_msvc
& 'C:\Program Files\Microsoft Visual Studio\18\Insiders\Common7\IDE\CommonExtensions\Microsoft\CMake\Ninja\ninja.exe' -C C:\opencv_build\src\build_msvc install
```

## 9. 下载 TensorRT SDK（需 NVIDIA 账号）

官网：https://developer.nvidia.com/tensorrt
选择：Version **11.2.1** → Platform **Windows** → 下载 `TensorRT-11.2.1.2.Windows.win10.cuda-13.0.zip`（1.9GB）
解压到 `C:\cpp_msvc_build\tensorrt\`

> 包含：`include\NvInfer.h`（头文件）、`lib\nvinfer_11.lib`（库）、`bin\trtexec.exe`（工具）

---

# 第四部分：C++ GPU 推理程序

## 10. 导出 YOLO ONNX 模型

```bash
cd "C:\Users\21404\Desktop\打工必备\各种作业跟项目\yololearn"
venv\Scripts\python -c "from ultralytics import YOLO; YOLO('yolo26n.pt').export(format='onnx', opset=17, imgsz=640)"
```
生成 `yolo26n.onnx`（9.5MB）

## 11. 用 trtexec 构建 TensorRT engine

```bash
cd C:\cpp_msvc_build
cp yolo26n.onnx .   # 复制 onnx 到工作目录
.\trtexec.exe --onnx=yolo26n.onnx --saveEngine=yolo26n_sdk.engine
```
生成 `yolo26n_sdk.engine`（约 11MB）

> 构建后验证：trtexec 报告 GPU latency 约 1.5ms，Throughput 约 650 qps

## 12. MSVC 编译 TensorRT C++ 程序

源码：`C:\cpp_msvc_build\tensorrt_yolo.cpp`

编译脚本 `build_trt_msvc.bat`：
```bat
@echo off
call "C:\Program Files\Microsoft Visual Studio\18\Insiders\VC\Auxiliary\Build\vcvars64.bat" >nul 2>&1
cd /d C:\cpp_msvc_build
set TRT_INC=C:\cpp_msvc_build\tensorrt\TensorRT-11.2.1.2\include
set TRT_LIB=C:\cpp_msvc_build\tensorrt\TensorRT-11.2.1.2\lib
set CUDA_INC=C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.3\include
set CUDA_LIB=C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.3\lib\x64
cl /std:c++17 /O2 /utf-8 /EHsc /I "%TRT_INC%" /I "%CUDA_INC%" /c tensorrt_yolo.cpp /Fo:tensorrt_yolo_msvc.obj
link /OUT:tensorrt_yolo_msvc.exe tensorrt_yolo_msvc.obj "%TRT_LIB%\nvinfer_11.lib" "%TRT_LIB%\nvinfer_plugin_11.lib" "%CUDA_LIB%\cudart.lib"
```

> ⚠️ **必须用 MSVC 编译**（不能 MinGW，MinGW 有 ABI 崩溃问题）

## 13. 运行 C++ GPU 推理

```bash
cd C:\cpp_msvc_build
tensorrt_yolo_msvc.exe yolo26n_sdk.engine bus.jpg
```

输出示例：
```
图片: 810x1080
推理耗时: 7.63 ms
  class=0 conf=0.90 box=[223.37,406.01,346.23,862.76]  # person
  class=5 conf=0.84 box=[1.16,228.77,807.59,757.06]    # bus
检测到 5 个目标
```

---

# 📍 所有文件位置一览

| 文件/目录 | 位置 |
|-----------|------|
| Python 虚拟环境 | `yololearn\venv\` |
| YOLO 模型 | `yololearn\yolo26n.pt` / `s` / `m` / `l` |
| ONNX 模型 | `yololearn\yolo26n.onnx` |
| Python engine | `yololearn\yolo26n.engine`（pip 版导出） |
| C++ CPU 项目 | `yololearn\cpp_yolo\`（含 build\cpp_yolo.exe） |
| onnxruntime 1.27 | `C:\cpp_msvc_build\ort127\` |
| OpenCV 源码+构建 | `C:\opencv_build\`（install = MinGW 版, install_msvc = MSVC 版） |
| CUDA Toolkit | `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.3\` |
| TensorRT SDK | `C:\cpp_msvc_build\tensorrt\TensorRT-11.2.1.2\` |
| C++ GPU 源码 | `C:\cpp_msvc_build\tensorrt_yolo.cpp` |
| C++ GPU 程序 | `C:\cpp_msvc_build\tensorrt_yolo_msvc.exe` |
| C++ GPU engine | `C:\cpp_msvc_build\yolo26n_sdk.engine` |
| MSVC 编译脚本 | `C:\cpp_msvc_build\build_trt_msvc.bat` |

---

# ⚠️ 关键注意事项（照抄必看）

1. **PyTorch 用 cu128 版**（不是 cu13，官方没出 cu13）；RTX 5060 需要 CUDA 12.8+
2. **OpenCV 源码放纯英文路径**（`C:\opencv_build`），中文路径编译失败
3. **C++ GPU 必须用 MSVC 编译**，MinGW 有 ABI 崩溃（onnxruntime 也不支持你的显卡）
4. **onnxruntime CUDA EP 不支持 RTX 5060 (sm_120)**——用 TensorRT 代替
5. **CUDA 安装时取消 Driver**（驱动已支持 CUDA 13）
6. **网络**：GitHub/官网下载需代理；pip 用国内镜像（清华）不需要代理
7. **TensorRT engine 用 trtexec 构建**（SDK 版），不要用 pip 版导出（序列化不兼容）
8. 运行 C++ GPU 程序时，TensorRT DLL 和 CUDA DLL 要放 exe 同目录
