# 🔧 YOLO 环境安装存档（傻瓜版复现指南）

> 本文档记录 **yololearn 项目**（Python YOLO + C++ CPU）所有成功配置的操作步骤，照着一步步做即可完整复现这套环境。
> 不含任何失败尝试，只留有效信息。
> 适用硬件：Windows 11 + NVIDIA RTX 5060 Laptop (Blackwell, sm_120) + 8GB 显存
>
> 📌 **C++ GPU（TensorRT）部署见** `C:\cpp_msvc_build\GPU环境安装存档.md`

---

## 🚦 0. 前置准备（先装这些，缺一不可）

> ⚠️ 本存档所有命令默认在 **VS Code 的 PowerShell 终端** 里执行（VS Code 打开文件夹后按 `Ctrl + \`` 呼出终端）。
> 只有在标注「Git Bash」的地方才需要切换（VS Code 终端右上角下拉框选 Git Bash）。

| 软件 | 下载/安装 | 装完验证（在 PowerShell 输入） |
|------|----------|-------------------------------|
| VS Code | https://code.visualstudio.com 下载安装 | 打开后能呼出终端即可 |
| Python 3.14.x | https://www.python.org/downloads 下载安装（⚠️ **安装时勾选 "Add Python to PATH"**） | `python --version` → 显示 3.14.x |
| Git | https://git-scm.com/download/win 下载安装 | `git --version` → 显示版本号 |
| MinGW-w64 | https://github.com/niXman/mingw-builds-binaries/releases 下载 x86_64 版，解压到 `C:\mingw64`，并把 `C:\mingw64\bin` 加进系统 PATH | 重开终端后 `g++ --version` 可用 |
| CMake | https://cmake.org/download 下载 Windows x64 安装包（安装时勾选 "Add to PATH"） | `cmake --version` → 显示版本号 |
| Ninja | 装 VS Code 扩展 "C/C++"，或下载 ninja.exe 放 `C:\mingw64\bin` | `ninja --version` → 显示版本号 |

> 💡 **PATH 怎么加**：Windows 搜索「编辑系统环境变量」→ 环境变量 → 用户变量选 Path → 编辑 → 新建 → 粘贴 `C:\mingw64\bin` → 确定，**重开终端**才生效。

---

## 📋 最终环境全景

| 组件 | 版本 | 用途 |
|------|------|------|
| Python | 3.14.2（venv 虚拟环境） | Python 检测环境 |
| PyTorch | 2.11.0+cu128 | Python GPU 推理（10.85ms） |
| OpenCV | 5.0.0 (Python) / 5.0.0 (C++ 源码编译) | 图像处理 |
| Ultralytics | 8.4.114 | 官方 YOLO 库（YOLO26） |
| onnxruntime | 1.27.0 (C++/CUDA13) | C++ CPU 推理 |
| MinGW g++ | 16.1 | C++ CPU 编译 |

**性能对比**：Python PyTorch GPU 10.85ms < C++ CPU 18ms

---

# 第一部分：Python YOLO 环境

> 🖥️ 以下命令在 **VS Code 默认的 PowerShell 终端** 执行（不要用 Git Bash）。

## 1. 创建虚拟环境

打开终端，进入项目目录：

```powershell
cd "C:\Users\21404\Desktop\打工必备\各种作业跟项目\yololearn"
C:\Users\21404\AppData\Local\Programs\Python\Python314\python.exe -m venv venv
```

> - 用系统稳定版 Python 3.14 创建（不要用 3.15 alpha，torch 不支持）
> - 如果 `python.exe` 路径跟你电脑不同，先运行 `where python` 找到实际路径再替换

**✅ 成功标志**：项目目录下出现 `venv\` 文件夹（里面有 Scripts 子目录）。

## 2. 升级 pip + 配置国内镜像（清华源）

```powershell
venv\Scripts\python -m pip install --upgrade pip
venv\Scripts\python -m pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

> - 第一行升级 pip 本身
> - 第二行把下载源换成清华镜像，**后面所有 pip 安装都走镜像，国内快且不用代理**

## 3. 安装 GPU 版 PyTorch（关键：CUDA 12.8 版）

```powershell
venv\Scripts\pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
```

> - `cu128` = CUDA 12.8 GPU 版（**必须选 GPU 版**，CPU 版是 `+cpu`）
> - RTX 5060 需要 CUDA 12.8+，cu128 正好满足
> - 约 2.5GB，下载需几分钟
> - ⚠️ 此命令走 PyTorch 官方源（不走清华镜像），**若慢/失败请先开梯子**（你的代理是 127.0.0.1:7897）
> - 💡 PyTorch cu128 自带 CUDA 运行库，Python GPU 推理无需单独装 CUDA Toolkit

**✅ 成功标志**：
```powershell
venv\Scripts\python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
```
应输出 `2.11.0+cu128 True`（True = GPU 可用）

## 4. 安装 OpenCV + Ultralytics

```powershell
venv\Scripts\pip install opencv-python ultralytics
```

> - opencv 5.0.0（图像处理）
> - ultralytics 8.4.114（官方 YOLO 库，含 YOLO26）

**✅ 成功标志**：
```powershell
venv\Scripts\python -c "import cv2, ultralytics; print(cv2.__version__, ultralytics.__version__)"
```
应输出 `5.0.0 8.4.114`

## 5. 下载 YOLO26 模型 + 第一次检测

模型文件 `.pt` 从 GitHub 自动下载。**GitHub 下载需代理**，先设置代理：

```powershell
$env:HTTP_PROXY="http://127.0.0.1:7897"
$env:HTTPS_PROXY="http://127.0.0.1:7897"
```

然后运行检测（ultralytics 官方示例图会自动下载 bus.jpg）：

```powershell
venv\Scripts\yolo predict model=yolo26n.pt source=https://ultralytics.com/images/bus.jpg
```

> - `source=...` 用官方图片 URL，**自动下载测试图**，不用自己找图片
> - 模型 `yolo26n.pt`(5.3MB) 自动下载到当前目录，下载一次永久使用
> - 想要更大的模型：`yolo26s.pt`(20MB) / `yolo26m.pt`(43MB) / `yolo26l.pt`(51MB)，换 model= 名字即可
> - 若模型下载卡住：也可浏览器打开 https://github.com/ultralytics/assets/releases 手动下载 `.pt` 放进项目目录再运行

**✅ 成功标志**：输出类似 `4 persons, 1 bus, 6.5ms`，结果存在 `runs\detect\predict-数字\`。

---

# 第二部分：C++ CPU 环境（onnxruntime）

> 🖥️ 步骤 6 在 **PowerShell**；步骤 7 的 OpenCV 编译在 **Git Bash**（会标注）。

## 6. 下载 onnxruntime 1.27

从 GitHub 下载（需代理）。二选一：

**方式 A（浏览器）**：浏览器打开下面链接下载（浏览器会走系统代理）：
```
https://github.com/microsoft/onnxruntime/releases/download/v1.27.0/onnxruntime-win-x64-gpu_cuda13-1.27.0.zip
```

**方式 B（PowerShell 命令行）**：
```powershell
cd C:\cpp_msvc_build
curl.exe -L -x http://127.0.0.1:7897 -o ort127.zip https://github.com/microsoft/onnxruntime/releases/download/v1.27.0/onnxruntime-win-x64-gpu_cuda13-1.27.0.zip
```

解压到 `C:\cpp_msvc_build\ort127\`，确认目录为：
`C:\cpp_msvc_build\ort127\onnxruntime-win-x64-gpu_cuda13-1.27.0\`（含 include 和 lib 文件夹）

> 💡 文件名含 gpu_cuda13 是指"依赖 CUDA 13 运行库"，但**只用它的 CPU 推理**（不启用 CUDA）。如果不想依赖 CUDA，可下载纯 CPU 版 `onnxruntime-win-x64-1.27.0.zip` 同样解压。

## 7. 编译 OpenCV 5.0（C++ 源码版，MinGW）

> 🖥️ ⚠️ **本小节必须切换到 Git Bash**（VS Code 终端右上角下拉框选 Git Bash），下面 `cd /c/...` 和 `\` 换行只在 Git Bash 有效。

### 7.1 下载源码（放纯英文路径，中文路径会编译失败）

```bash
cd /c/opencv_build
git clone --depth 1 --branch 5.0.0 https://github.com/opencv/opencv.git src
```

> ⚠️ **必须放纯英文路径**（如 `C:\opencv_build\src`），中文路径会导致 NASM 汇编编译失败
> 💡 若 git clone 卡住，先执行 `git config --global http.proxy http://127.0.0.1:7897`（用完可 `git config --global --unset http.proxy` 恢复）

### 7.2 配置构建（只编核心模块）

```bash
cd /c/opencv_build/src
C:/mingw64/bin/cmake.exe -G Ninja -S . -B build \
  -DCMAKE_CXX_COMPILER=C:/mingw64/bin/g++.exe \
  -DCMAKE_C_COMPILER=C:/mingw64/bin/gcc.exe \
  -DCMAKE_MAKE_PROGRAM=C:/mingw64/bin/ninja.exe \
  -DCMAKE_BUILD_TYPE=Release \
  -DBUILD_SHARED_LIBS=OFF \
  -DBUILD_LIST=core,imgproc,imgcodecs,videoio,calib3d,highgui \
  -DWITH_FFMPEG=OFF -DWITH_V4L=OFF -DWITH_GSTREAMER=OFF -DWITH_GTK=OFF \
  -DWITH_OPENCL=OFF -DWITH_CUDA=OFF -DWITH_OPENEXR=OFF \
  -DBUILD_opencv_apps=OFF -DBUILD_TESTS=OFF -DBUILD_PERF_TESTS=OFF -DBUILD_EXAMPLES=OFF \
  -DBUILD_opencv_python3=OFF -DBUILD_opencv_java=OFF \
  -DCMAKE_INSTALL_PREFIX=C:/opencv_build/install
```

### 7.3 编译并安装

```bash
cd /c/opencv_build/src
C:/mingw64/bin/ninja.exe -C build
C:/mingw64/bin/ninja.exe -C build install
```

**✅ 成功标志**：
- 命令执行完 exit 码为 0（无红色报错）
- `C:\opencv_build\install\` 下出现 include 和 x64\mingw\staticlib 文件夹
- 耗时约 3-10 分钟，**中间大量编译输出是正常的**，耐心等

> ⚠️ MinGW 版无 FFmpeg，视频只能读 AVI（mp4 需用 Python 版或 GPU 版）

## 8. 编译 C++ CPU 程序（cpp_yolo.exe）

源码在项目 `cpp_yolo\` 目录，用 CMake 编译：

> 🖥️ 本步在 **VS Code 默认 PowerShell** 执行。

```powershell
cd "C:\Users\21404\Desktop\打工必备\各种作业跟项目\yololearn\cpp_yolo"
C:\mingw64\bin\cmake.exe -G Ninja -S . -B build -DCMAKE_CXX_COMPILER=C:/mingw64/bin/g++.exe -DCMAKE_C_COMPILER=C:/mingw64/bin/gcc.exe -DCMAKE_MAKE_PROGRAM=C:/mingw64/bin/ninja.exe -DCMAKE_BUILD_TYPE=Release
C:\mingw64\bin\ninja.exe -C build
```

**✅ 成功标志**：`cpp_yolo\build\cpp_yolo.exe` 出现。

运行验证（PowerShell）：
```powershell
cd "C:\Users\21404\Desktop\打工必备\各种作业跟项目\yololearn"
cpp_yolo\build\cpp_yolo.exe cpp_yolo\..\bus.jpg yolo26n.onnx
```
> 若没有 bus.jpg，先跑一遍第 5 步让官方图下载，或把任意图片命名为 bus.jpg 放项目目录。
> 输出显示 `检测到 5 个目标` 即成功。

---

# 📍 文件位置一览

| 文件/目录 | 位置 |
|-----------|------|
| Python 虚拟环境 | `yololearn\venv\` |
| YOLO 模型 | `yololearn\yolo26n.pt` / `s` / `m` / `l` |
| ONNX 模型 | `yololearn\yolo26n.onnx` |
| C++ CPU 项目 | `yololearn\cpp_yolo\`（含 build\cpp_yolo.exe） |
| onnxruntime 1.27 | `C:\cpp_msvc_build\ort127\` |
| OpenCV 源码+构建（MinGW） | `C:\opencv_build\`（install = MinGW 版） |

> 🔗 C++ GPU 相关文件（TensorRT/CUDA/engine）在 `C:\cpp_msvc_build\`，见 `GPU环境安装存档.md`

---

# ⚠️ 关键注意事项（照抄必看）

1. **PyTorch 用 cu128 版**（不是 cu13，官方没出 cu13）；RTX 5060 需要 CUDA 12.8+
2. **只有编译目录必须纯英文路径**：`C:\opencv_build`、`C:\cpp_msvc_build`（MinGW/MSVC/cmake 参与编译时中文路径会失败）；纯 Python 的 yololearn 目录允许中文路径
3. **网络**：GitHub 下载需代理（终端 curl/git 要手动加代理，见对应步骤）；pip 走清华镜像不需要代理
4. **C++ CPU 视频格式限制**：MinGW 版 OpenCV 无 FFmpeg，视频检测只能读 AVI
5. **所有 `venv\Scripts\python` 命令都要先 `cd` 到项目目录**，新开终端先 `cd "C:\Users\21404\Desktop\打工必备\各种作业跟项目\yololearn"`
